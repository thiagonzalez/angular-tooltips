import { TooltipPositionDirective } from './tooltip-position.directive';

describe('TooltipPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipPositionDirective();
    expect(directive).toBeTruthy();
  });
});
